# CLAUDE.md — QG du séjour Argelès-Gazost 2026

## Mission immédiate (à exécuter par Claude Code)
1. Lire `docs/source-verite_qg-argeles_2026-07-26.md` (source de vérité de la session d'origine).
2. ~~Créer et pousser le dépôt~~ — fait : dépôt `Activit-s-camping-Argeles-2026`, branche `claude/new-session-kzzpl2`.
3. ~~Remplacer `TRIP` par une chaîne aléatoire~~ — fait (`sejour-` + `openssl rand -hex 16`).
4. Tester en local : `node --check server.js` puis `npm install` + `PORT=10123 node server.js` + `curl localhost:10123/` (HTML) et `curl "localhost:10123/api/sync?trip=test"` (500 attendu tant que `DATABASE_URL` n'est pas définie).
5. Guider l'utilisateur pas à pas : création base **PostgreSQL Render** (plan Free, essai ~1 mois) → copier l'Internal Database URL → Render → New + → Blueprint → ce dépôt → variable `DATABASE_URL` → Apply (voir README). L'URL de base contient le mot de passe : jamais dans le dépôt.
6. Vérifier sur l'URL Render : page OK, badge « ✓ partagé », coche visible depuis un 2e appareil. Donner l'URL finale.

## Invariants (ne pas casser)
- Tout en français. Mobile-first, cibles tactiles >= 44 px.
- Les animations du camping s'affichent TOUJOURS « en extra », en plus des sorties — jamais à la place.
- Aucune donnée inventée : planning = photos du panneau ; prix/horaires = sources du guide ; sinon marquer [à vérifier].
- Pas de localStorage / sessionStorage : état partagé via /api/sync, contrat {state:{clé:{v,t}}, resetAt}, fusion horodatée par clé, resetAt purge les clés antérieures. Stockage : table PostgreSQL `sync_state` (une ligne par `trip`, colonne `data` JSONB), créée automatiquement au premier appel.
- `TRIP` reste une chaîne aléatoire longue (pas d'authentification).
- Déploiement cible : Render Web Service (`server.js`, Node >= 18, une seule dépendance npm : `pg`) + base PostgreSQL Render via env `DATABASE_URL`. La variante Vercel n'existe plus ici (voir dépôt roadbook) ; la variante Upstash Redis a été remplacée le 26/07 (décision utilisateur).

## État au 04/08/2026 (v1.38)
- Vues : Semaine (météo du jour partagée + camping en extra + soirées), Sorties (28 fiches dépliables : photo Wikipédia avec repli, accès, bouton itinéraire Google Maps, horaires, durée idéale famille, points forts), Missions, Infos (RAZ double appui). Score famille, niveaux Marmotte → Ours.
- Planning camping transcrit : 26/07 → 07/08 (3 publics), 219 activités. Le 08/08 (départ) affiche un message dédié, pas un placeholder.
- v1.2 : backend porté de Vercel vers Render (`server.js` sert `/` et `/api/sync`).
- v1.3 : persistance portée d'Upstash Redis vers **PostgreSQL Render** (`DATABASE_URL`, TLS auto pour les URL externes `render.com`, pool `pg` avec gestionnaire d'erreurs) ; contrat API et front inchangés ; `render.yaml` passe à `buildCommand: npm install`.
- v1.5 : sorties consolidées avec le panneau « Explore & Discover » du camping (photo du 26/07) — adresses exactes, bus LIO 965 / navettes estivales, durées affichées, et **5 nouvelles fiches** (parc du Lutin Pêcheur, balade avec un berger à la ferme Maria Blanca, boucles de l'Arrieulat et du Balandrau, musée de Cire) + nouvelle catégorie `R` 🚶 Balades à pied → **27 fiches, 6 catégories**. Onglet **🗺️ Carte** : Leaflet + tuiles OSM chargés à la demande (repli hors ligne), repères émoji colorés par catégorie autour du camping des 3 Vallées, popup → « Voir la fiche » (saute sur la fiche dépliée), marqueurs grisés quand la sortie est faite. Les coordonnées `ll` des fiches sont indicatives (affichage carte uniquement) ; les itinéraires restent au géocodage Google Maps par nom (décision d'origine inchangée). La carte vit hors de `#app` pour survivre aux re-rendus de la synchro.
- v1.6 : onglet **🥾 Randos** — 16 itinéraires famille (17 depuis la v1.13) (D+ ≤ 500 m, ≤ demi-journée, enfant de 5 ans), dont 5 à pied depuis Argelès, **vérifiés en ligne le 26/07/2026** (Rando Vallées de Gavarnie, AllTrails, Komoot, Visorando, Cirkwi, OT — lien source sur chaque fiche, non recoupé = [à vérifier]). Fiches : bandeau distance/D+/durée/âge, itinéraire étape par étape numéroté, **schéma SVG indicatif** (points-clés reliés — pas un tracé GPS), boutons 🧭 départ / 🗺️ grande carte (tracé mis en avant via `focusRando`) / 🔗 tracé externe. Intégré au jeu : cochables +10 pts (clés `done:`, dédoublonnées avec les sorties via `score()`), votables (groupe `H` 🥾 Randos dans Favoris via `FAVCATS`/`FAVLIST`), marqueurs 🥾 sur la grande carte. `arrieulat` et `balandrau` partagent leurs ids avec les fiches Sorties (une seule coche/vote pour les deux vues). Barre à 7 onglets.
- v1.7 : bloc **🌙 Soirées** dans la vue Semaine — mini-disco quotidienne 21h15 (15 min, grande scène, info utilisateur du 26/07) + soirées transcrites des captures de l'appli Yelloh (27/07 Bal mousse · 28/07 Spectacle Hits Rock · 29/07 Soirée Happy/Duo 20h au restaurant Le Brasero puis Fête du Sud-Ouest 21h30 (placée le 29 sur correction utilisateur, v1.7.3) · 30/07 Spectacul'Art · 31/07 Tom et Johnny · 01/08 Samedi tout est permis — 21h30, grande scène sauf mention). Constante `SOIREES` séparée de `CAMP` (source différente), accordéon ouvert par défaut, affiché aussi sur les jours sans planning (semaine 2). Nouvelle fiche sortie **🪂 Baptême de parapente** (28 fiches) d'après le document Axiom Parapente : RDV mercredi 29/07 16h, décollage col de Couraduque, atterrissage avant Aucun (plan « Las Poueyes »), contact Michel 07 86 62 89 28, baskets obligatoires — répercuté dans la note du 29/07.
- v1.7.1 : mission `md:4` remplacée (vélo voie verte → « 🪂 Sortie : le baptême de parapente ») + corrections du bug d'ajout de mission signalé sur téléphone : (1) la synchro périodique re-rendait la vue et effaçait la saisie en cours — `rendre()` préserve désormais valeur, focus et curseur des champs `#nm`/`#nmb` ; (2) la barre d'onglets recouvrait le bas de page sur iPhone — `viewport-fit=cover` + `env(safe-area-inset-bottom)` sur `nav` et `body` ; (3) la touche Entrée valide l'ajout (mission et membre), champ vidé avant `put()` (fonctions `ajouterMission`/`ajouterMembre`).
- v1.8 : **tout est cochable et daté**. (1) Chaque ligne du planning (3 publics + soirées, mini-disco comprise) porte une case « réalisée » — clé `ca:<date>:<public>:<heure>:<slug>` (stable face aux insertions), partagée comme le reste. (2) Toute coche horodate la réalisation à la **date du jour** dans une clé compagnon `dt:<clé>` — **corrigeable** ensuite via le sélecteur de date de l'onglet Bilan (les activités du planning sont datées de leur jour prévu par défaut). (3) Nouvel onglet **🏆 Bilan** : synthèse groupée par jour (sorties, randos, animations, soirées, missions), points cumulés, ligne tactile vers la fiche. (4) Les plans de journée ne reproposent plus une sortie/rando déjà réalisée. (5) Missions auto-validées quand l'activité liée est cochée (table `MISSLIEN` : padel, tir à l'arc, boucles, Hautacam, parapente, créas, marché, grands sites, rapaces) — jamais dé-cochées automatiquement. Barre à 8 onglets.
- v1.9 : bloc **🎯 Notre programme du jour** dans la vue Semaine — la famille compose la journée elle-même : menu déroulant des sorties/randos **non déjà suggérées et non réalisées** (groupées par catégorie) ou champ libre « activité à nous » (bouton + ou touche Entrée), retrait par ✕. Clé partagée `pj:<date>` (tableau `{i:<idSortie>}` ou `{x:"texte", id:"p<ts>"}`). Les lignes du plan météo **et** du programme portent désormais la case « réalisée » à côté de la zone tactile qui ouvre la fiche (`ligneIdee()` commune ; les sorties/randos utilisent la clé `done:` habituelle, donc cohérence fiche/carte/score/favoris). Les activités libres entrent dans `catalogueCA()` (groupe `p`) et donc dans le Bilan, sous le type **📝 Ajoutées au programme**.
- v1.10 : **effets de célébration**. `fete(ev, emojis, n)` remplace l'ancien `confetti()` : gerbe de particules projetées en cercle depuis le point touché (émojis, tailles, angles et durées variés). `toast(titre, sous, classe, ms)` affiche un bandeau auto-effacé dans `#toasts` (`aria-live="polite"`). Coche d'une mission (manuelle, perso ou **auto-validée** par une activité) → confettis 🎉🏅 + bandeau « Mission accomplie / débloquée · +2 pts » + pulsation de la barre de score (`pulseScore()`), les missions auto étant annoncées en cascade décalée. Sorties/randos et activités du planning ont leur propre variante ; les votes ❤️ leur mini-gerbe. Changement de palier (Marmotte → Isard → Vautour → Ours) détecté dans `majScore()` → grande gerbe 🏆 + bandeau spécial. Tout est neutralisé par `prefers-reduced-motion` (règle globale existante).
- **Refonte ergonomique & esthétique (demande utilisateur du 03/08)** — plan en dix étapes, volet ergonomie d'abord (le séjour finit le 08/08), volet esthétique ensuite. Décisions actées : on **garde les 8 onglets** (mieux calibrés) plutôt que de passer à 5 en plein séjour ; mode nuit **automatique** sans bouton mémorisé (l'invariant « aucun stockage navigateur » l'interdit, et le mettre dans l'état partagé imposerait le thème d'un téléphone à toute la famille) ; lecture chronologique de la journée **en option**, les trois blocs restent.
- v1.38 : **quatre jauges de jeux, quatre formes** (demande utilisateur du 04/08). Les quatre paliers étaient rendus par la même barre horizontale ; on ne les distinguait qu'en lisant les chiffres. Chacun prend sa forme et son thème : `md:11` 10 jeux → **🛂 Passeport Ludique** (horizontale segmentée, dix cases de tampons) · `md:13` 20 jeux → **🗄️ Kallax** (grille 5×4 — un Kallax fait justement 20 cases — boîtes en couleurs de catégorie cyclées) · `md:12` 20 parties → **🧩 Meeple Bois** (anneau SVG piloté par `stroke-dashoffset`, silhouette de meeple au centre) · `md:14` 30 parties → **🌡️ Tableflip Risk** (thermomètre vertical). Disposition en **grille 2×2**. `JSEUILS` gagne `forme` / `nom` / `accroche` / `ok` — **les intitulés des quatre missions ne changent pas** (liste, Bilan, bandeaux), c'est eux qui disent quoi faire ; une non-régression le vérifie. Chaque vignette porte `data-forme` / `data-pc` / `data-atteint` : **point d'accroche indépendant de la forme**, la largeur d'un `<i>` ne voulant plus rien dire sur un anneau ou une grille — `jalons.mjs` (36 contrôles) est réécrit autour. Deux pièges corrigés à la capture : la grille Kallax débordait de sa vignette (boîte de dessin portée à 84 px, grille à 100 px de large), et **le mercure du thermomètre montrait le haut du dégradé — donc le rouge — dès 30 %** ; le `<i>` est désormais ancré en bas de son masque, si bien que la couleur chauffe réellement avec la progression. Deux tokens ajoutés (`--bois`, `--froid`) avec leurs variantes de nuit. À l'impression, les formes disparaissent et les quatre chiffres passent sur une ligne : c'est le chiffre qui doit survivre au papier, pas le dégradé.
- v1.37 : **vue de synthèse** (demande utilisateur du 03/08). Bouton 🧭 dans l'en-tête → `vue = "synthese"`, et retour à l'onglet d'où l'on vient (`vueAvant`, local). **Pas un neuvième onglet** : on reste à 8. L'écran donne, dans l'ordre : où on en est (points, activités, jours restants, palier + points avant le suivant) ; **aujourd'hui** — programme du jour non fait, idées du plan météo non faites, animations « en famille » du jour, soirées — ; les compteurs (sorties, randos, missions, jeux, animations) ; trois prochaines missions ; cinq dernières réalisations. Chaque bloc porte un raccourci `allervue` vers son onglet. **Référence temporelle = le jour réel (`dateDe()`), pas `jourSel`** : la question posée est « maintenant ». **Invariant dur, le même qu'au fil de la journée** : tout passe par `ligneIdee()` et `ligneCA()`, donc par les mêmes clés — cocher depuis la synthèse compte exactement pareil, cascade de missions auto comprise. `synthese.mjs` (21 contrôles) le vérifie clé par clé et score en main.
- **Bug trouvé par la suite : l'en-tête vibrait.** Il se compacte au-delà d'un seuil de défilement ; or compacter raccourcit la page, et le navigateur décale alors le défilement d'une soixantaine de pixels pour garder le point de lecture — ce qui repassait aussitôt de l'autre côté du seuil. Avec un seuil unique (28 px depuis la v1.11), puis avec deux seuils trop rapprochés (12/48), la bascule se mordait la queue et la page tremblait. **L'écart entre les deux seuils doit dépasser la différence de hauteur entre les deux états** : 8 px pour ré-étendre, 96 px pour compacter. Le bandeau de progression de la v1.32 avait creusé cet écart de hauteur et rendu la boucle atteignable. Non-régression dans `ergonomie.mjs` : la hauteur de l'en-tête doit se stabiliser à 0, 10, 30, 60, 100 et 200 px de défilement. Deux fragilités de la suite corrigées au passage — `e2e-qg.mjs` tue son serveur même en cas de plantage (sinon l'orphelin garde le port et le run suivant dialogue avec lui sans le savoir) et attend la condition de synchro plutôt qu'une durée fixe.
- v1.36 : **bouton de bascule jour/nuit** (demande utilisateur du 03/08). Le mode nuit ne suivait que le réglage du téléphone. Une `@media` ne se surchargeant pas depuis un bouton, le bloc sombre **devient une classe pilotée par JS** : `html.nuit`, posée par `majTheme()` qui combine `prefereNuit()` (le téléphone) et `themeManuel` (le choix de cette page, `null` tant qu'on n'a rien dit). Un seul jeu de valeurs — pas de duplication — et la spécificité y gagne (`html.nuit .bn-G` passe devant `.bn-G` où qu'elle soit dans la feuille, ce qui était le piège de la v1.30). `color-scheme` est posée avec, **sinon les sélecteurs de date natifs du Bilan restent blancs en nuit forcée**. `theme-color` passe de deux balises `media` à une seule, mise à jour en JS. Le changement de réglage du téléphone en cours de session est suivi (`matchMedia().addEventListener`), sauf après un choix manuel. **Aucune mémoire** : décision utilisateur — l'invariant « aucun stockage navigateur » couvre aussi les cookies, donc le thème repart du réglage du téléphone à chaque rechargement. Bouton 🌙/☀️ dans l'en-tête (l'icône annonce ce qu'on obtient), boîte 36 px et zone de contact 44 px. `theme.mjs` (18 contrôles) dont **localStorage, sessionStorage et cookies vérifiés vides après plusieurs bascules**. La pellicule confirme que la conversion est fidèle : en sombre, aucun token ni aucune couleur n'a bougé.
- v1.35 : **feuille d'impression**. `@media print` : tout ce qui sert à naviguer disparaît (onglets, retour en haut, barre de progression, filtres, sélecteur météo, zones d'ajout, confettis, grain de papier, carte), l'en-tête reste mais à plat, noir sur blanc, cartes à hairline sans ombre ni ruban, `break-inside:avoid` sur les cartes et les lignes, sélecteur natif des dates masqué, et **les liens externes déplient leur adresse en note** (`a[href^="http"]::after{content:" (" attr(href) ")"}`) — on ne clique pas sur du papier. Le Bilan devient le souvenir imprimable du séjour et le programme d'une journée s'affiche dans le mobil-home. `impression.mjs` (14 contrôles) vérifie le rendu en média `print` **et** produit deux PDF A4.
- v1.34 : **visuel des fiches**. (1) **Photos sans saut de page** : `.s-vis` en `aspect-ratio:16/10` — la boîte a sa forme **avant** que la photo arrive, et le dégradé de catégorie sert d'abord de fond d'attente puis de repli définitif. La photo apparaît en fondu (`.vu` posée par un écouteur `load` **en capture**, l'événement ne bouillonnant pas). Vérifié en servant une photo volontairement lente : ni la boîte ni ce qui suit dans la fiche ne bougent d'un pixel. (2) **Nom incrusté** sur un voile dégradé, lisible quelle que soit la photo dessous ; **crédit photo** traité en légende. (3) **Filigrane** : silhouette de sommets en `::before` sur les six dégradés `bn-*`, qui n'étaient que des diagonales bicolores. (4) **Quatre états vides illustrés** (`DESSINS` : sommets, loupe, carnet, dé — SVG inline `aria-hidden`, `currentColor`) remplacent quatre pavés de texte : Bilan vide, filtres sans résultat, programme du jour vide, aucun jeu. (5) **Bug préexistant trouvé par la pellicule** : `.rev.vu{opacity:1}` (révélation au défilement, v1.11) est déclarée après `.sortie.fait{opacity:.62}` — à spécificité égale la dernière gagne, donc **une sortie déjà faite reprenait son opacité pleine dès qu'on revenait sur l'onglet**. Corrigé en préfixant `article.` ; non-régression dans `visuel.mjs`. La signature de `pellicule-ui.mjs` ignore désormais les classes d'animation (`rev`, `vu`, `swap`…), qui varient selon l'instant du relevé.
- v1.33 : **matière et mouvement**. (1) **Grain de papier** : bruit fractal `feTurbulence` en SVG inline (data URI, ~300 octets, aucune requête) posé en voile `position:fixed` sous l'en-tête et la barre d'onglets — c'est lui qui donne au papier crème sa matière, jusque-là un aplat parfaitement lisse. **Neutralisé en thème sombre** (il y lit comme du bruit). (2) **Transitions d'état** : cocher barre le texte en fondu, les cases et boutons d'état changent de couleur en douceur, la pastille du jour a un petit ressort à la sélection. (3) **Transitions de vue natives** (`document.startViewTransition`, Chrome 111+/Safari 18+) **uniquement au changement d'onglet** — là où l'appelant n'a pas besoin du DOM tout de suite. Partout ailleurs (ouverture d'une fiche suivie d'un `scrollIntoView`, tests) `rendre()` reste synchrone. `animerEntree()` saute alors `#app.swap`, qui reste le repli. **Conséquence assumée et documentée** : après un clic d'onglet, le DOM se met à jour une image plus tard ; 26 assertions d'`e2e-qg.mjs` lisaient le DOM immédiatement et attendent désormais. (4) **Pas de squelettes de chargement** : décision déjà prise en v1.27 — la page se rend instantanément dès que le HTML arrive, l'attente est en amont, côté requête ; c'est le bandeau `#reveil` qui répond au vrai problème.
- v1.32 : **rythme et mise en page**. (1) **En-tête illustré** : silhouette de sommets + soleil en SVG inline (data URI, ~380 octets, aucune requête), même vocabulaire graphique que `icone.svg`. (2) **Élévation lisible** : le ruban adhésif et l'ombre appuyée passent de *toutes* les cartes à `#app > section.fiche:first-of-type` — sur toutes, l'effet s'annulait. (3) **Deux colonnes au-delà de 680 px** pour Sorties et Randos (`.grille`, `grid-template-columns:1fr 1fr`) ; la vue Semaine reste en une colonne, c'est une lecture chronologique. Favoris reste également en une colonne (lignes compactes, pas des cartes). (4) **Titres de catégorie collants** sous la barre de filtres, via une seconde variable `--h-collant` (en-tête + barre) tenue à jour par `majHauteurEntete()` ; `scroll-margin-top` des fiches recalé dessus. (5) **Bandeau de progression** `#prochain` : « Encore N points avant 🦅 Vautour », masqué quand l'en-tête se compacte. (6) **Cibles tactiles** : les trois tailles sous 44 px relevées à l'étape 1 sont traitées — la boîte visible reste petite (36×36, 34×34) mais un `::after` en inset négatif porte la **zone de contact** à 44 px, sans grossir le dessin ni décaler la mise en page. `ergonomie.mjs` mesure désormais la zone de contact (pseudo-élément compris) et non la boîte, et c'est devenu une assertion.
- v1.31 : **système typographique**. Échelle fluide `--t1/--t2/--t3` en `clamp()` (titre du QG, titres de carte, noms de fiche), interlignage différencié (1,25 sur les titres, 1,5 sur le texte courant — c'était 1,45 partout), `font-variant-numeric: tabular-nums` sur tout ce qui affiche un nombre qui change (chips horaires, badges, compteurs, scores, dates du Bilan) — ils se décalaient d'un rendu à l'autre. **Passe de typographie française** : apostrophes courbes (22 apostrophes échappées `\'` remplacées) et **espace fine insécable U+202F devant `! ? : ;`** — 33 lignes retouchées. **Périmètre strictement limité aux textes d'interface** : la passe (`passe-typo.py` du scratchpad) repère les blocs de données par bornes, ne transforme que l'intérieur des littéraux de chaîne (le code, les sélecteurs CSS et les commentaires gardent leurs `:`) et **refuse d'écrire si une empreinte de zone de données a bougé**. `typo.mjs` vérifie en continu les 9 empreintes (`CAMP`, `NOTES`, `SOIREES`, `SORTIES`, `RANDOS`, `MISSIONS`, `METEO`, `ICOMISS`, `CATS`) et parcourt le rendu à la recherche d'une espace sécable restante, en excluant les libellés qui viennent des données.
- v1.30 : **mode nuit automatique** (`prefers-color-scheme`, sans bouton — l'invariant « aucun stockage navigateur » interdit de mémoriser un choix, et le ranger dans l'état partagé imposerait le thème d'un appareil à toute la famille). L'essentiel tient dans l'inversion de `--craie` (surface) et `--encre` (encre) : les règles qui les emploient **par paire** s'inversent correctement d'elles-mêmes. Trois exceptions nommées : `--sombre`/`--sur-sombre` (en-tête et bandeaux, qui doivent rester sombres — les inverser donnerait une barre claire en pleine nuit) et `--sur-soleil` (le jaune ne change pas, son texte doit donc rester foncé alors que `--encre` s'éclaircit). Turquoise et sapin remontés d'un cran en nuit, dégradés `bn-*` redéfinis en teintes de nuit, tuiles OSM laissées claires avec repères blancs par-dessus (un filtre fausserait les couleurs de la carte). `theme-color` dédoublée par `media`. **Piège rencontré** : le bloc `@media` était déclaré avant les règles qu'il surcharge — à spécificité égale, la règle claire gagnait et les vignettes restaient éblouissantes. Le bloc vit désormais en fin de feuille. **Audit de contraste automatique** ajouté à `pellicule-ui.mjs` (WCAG AA, en traversant les parents transparents pour trouver le fond réel) : il a trouvé 4 fautes que je venais d'introduire (titres de section colorés, libellé du jour), 2 propres au thème sombre (badge de synchro, pastille de podium) et 6 préexistantes. Toutes corrigées — les tokens `--cat-*` cessent d'être des alias des couleurs d'accent (ils servent de couleur de texte, donc doivent tenir 4,5:1 sur le papier crème), `--gave-t`/`--braise-t` portent le texte sur fond clair, `--texte-4` est assombri. **Les deux thèmes passent AA.**
- v1.29 : **confort de lecture**. (1) **Fil de la journée** — bascule `🗂️ Par public` / `🕘 Fil de la journée` dans la vue Semaine (état `filJour`, local). Le fil fond les 3 publics et les soirées en une frise horaire découpée matin / après-midi / soirée, chaque ligne portant la pastille de son public. **Invariant dur : `ligneCA()` est partagée par les deux lectures et dérive la même clé `cleCA()`** — mêmes coches, même score, même Bilan ; c'est une lecture de plus, pas un état de plus. L'encart « toujours en extra » est conservé dans les deux modes. (2) **Bilan** : synthèse en tête (activités, jours, points, palier + points restants avant le suivant), filtre par type (pastilles `fbil`, état local `fBilan`), et **« Copier le récap »** — texte prêt à envoyer, via `navigator.clipboard` avec repli `textarea` + `execCommand` quand le presse-papier n'est pas accessible. (3) L'échelle des paliers sort de `majScore()` : `NIVEAUX` / `SCOREMAX` / `palier(s)` sont désormais lisibles par le Bilan (et par le bandeau de progression de l'étape 6). Vérifié par `fil-jour.mjs` (21 contrôles) dont l'égalité des clés entre les deux lectures. **Deux assertions anciennes mises à jour** (`e2e-qg`, `audit-coherence`) : elles cherchaient la phrase « N points cumulés », que la synthèse remplace — l'invariant testé (total du Bilan = score de l'en-tête) est inchangé, seule sa lecture dans le DOM a bougé.
- v1.28 : **trouver vite** — 28 fiches sorties et 17 randos n'avaient aucun filtre. Barre `.filtres` **collante** sous l'en-tête (`--h-entete` publiée en JS car l'en-tête se compacte : une valeur figée laisserait un trou ou un recouvrement), avec champ de recherche (nom, points forts, description, préparation ; comparaison `sansAccent()` — « betharram » trouve « Bétharram »), pastilles de catégorie **cumulables**, quatre critères (`⬜ pas encore faite`, `❤️ votée`, `⏱ < 2 h`, `🚗 ≤ 15 min` / `⛰️ D+ ≤ 200 m`), compteur `n/total` **dans la ligne de recherche** (la barre est collante : chaque pixel est pris à la lecture, elle tient sous 110 px) et état vide rédigé. État `FILTRE` **local à la page** — filtrer est un geste personnel, rien à remonter dans l'état partagé ; `#rech` rejoint la liste de préservation de `rendre()` (sinon la synchro viderait la saisie, bug v1.7.1). Durées et trajets étant du texte libre accumulé au fil des versions (« 1h20 », « 50-55 min », « ½ j », « à pied », « plat »), `enMinutes()` et `enMetres()` en tirent un seuil grossier — **la suite de tests vérifie qu'aucune fiche ne produit de valeur aberrante**. Visuel : liseré de catégorie sur chaque carte et son titre de section (mêmes tokens que la carte Leaflet), vignette 46 px reprenant le dégradé `bn-*` existant — du rythme sans une seule requête réseau.
- v1.27 : **repères immédiats**. (1) `defJour()` passe de `toISOString()` à `dateDe()` : la date UTC ouvrait l'app sur la **veille** entre minuit et 2 h du matin (UTC+2 l'été). (2) La bande des jours se **centre** sur le jour choisi — `centreBandeJours()` écrit `scrollLeft` (et non `scrollIntoView`, qui ferait aussi remonter la page à cause de `scroll-behavior:smooth`), **uniquement au changement de jour ou d'onglet** : la synchro re-rend toutes les 15 s et reprendrait sinon la main sur le défilement horizontal en pleine lecture (garde `dernierJour` à côté de `derniereVue`). (3) Repère **« aujourd'hui »** : liseré braise + libellé de jour en braise sur la date réelle, jours passés atténués (`.auj` / `.passe`, comparaison de chaînes ISO), `aria-current="date"` sur le jour consulté. (4) **Bandeau `#reveil`** : tant que le premier échange n'a pas abouti, la page affiche l'état vide (0 pt, rien de coché) et on croit tout perdu — le bandeau explique le réveil de la base Render au bout de 4 s, ou l'injoignabilité. **Pas de squelettes** : la page se rend instantanément dès que le HTML arrive, l'attente est en amont, côté requête. (5) Bouton **retour en haut** au-delà de 600 px. (6) Barre d'onglets : libellés 9 → 10 px sans retour à la ligne (interlettrage −0,2 px), `aria-current="page"`, `aria-label` sur `nav`, badge de synchro en `role=status`. Vérifié par `ergonomie.mjs` (21 contrôles) ; `pellicule-ui.mjs` ne relève que les changements voulus.
- v1.26 : **fondations CSS invisibles**. Une trentaine de valeurs codées en dur deviennent des tokens sémantiques (`--surface`, `--surface-2`, `--surface-fait`, `--surface-svg`, `--eau`, `--trait`, `--texte-2/3/4`, `--miel`, `--encre-2`, `--encre-nuit`, `--or-fonce`, `--alerte`) — c'est ce qui rendra le mode nuit possible en un seul endroit. Dix rayons d'arrondi ramenés à quatre (`--r0` 4 px, `--r1` 8, `--r2` 12, `--r3` 16, `--rp` pastille) et dix ombres à six tokens : **seul changement visuel de l'étape** (cartes 14 → 16 px, petits contrôles 9-10 → 8 px). Couleurs de catégorie : `CATCOL` (JS) et la légende écrite en dur dans le HTML répétaient les mêmes hexadécimaux — source unique désormais, les tokens `--cat-<lettre>` lus par les repères Leaflet via `catCouleur()` et par une légende générée depuis `CATS`. Nouvel outil `pellicule-ui.mjs` : relève pour chaque élément des 8 vues, en 390/414/768 px, sa géométrie et 23 styles calculés, et compare **par identité d'élément** (pas par position — insérer un bandeau décalerait sinon toutes les lignes). Verdict de l'étape : `borderRadius` sur 495 éléments, et rien d'autre.
- v1.25 : **date de la partie de jeu** (demande utilisateur du 03/08). Champ `#djs` (`type=date`, borné à `DAYS`) en tête de la zone d'ajout du bloc Jeux, alimenté par la variable locale `dateJeu` (choix de saisie, pas un état partagé — comme `moi`) avec repli sur la date du jour puis `jourSel`. Les **deux** chemins d'ajout (menu `#seljs` des jeux déjà joués, champ `#njs` d'un nouveau jeu) appellent `poseJeuSurJour(idJeu, date)` : entrée `{j:<id>}` dans `pj:<date>` **et** coche `ca:<date>:p:--:<id>` + `dt:` — c'est cette coche qui fait apparaître la partie au Bilan. Rejouer le même jeu le même jour n'ajoute pas de 2e ligne (garde `some(it.j === id)`), seulement une partie au compteur. Les **`−`/`+` de chaque ligne restent de purs correcteurs de compteur** et ne touchent pas au planning : la ligne sert à rattraper une saisie, la zone d'ajout à déclarer une partie jouée. Vérifié par `date-jeu.mjs` du scratchpad (20 contrôles : deux dates différentes, rejeu le même jour, planning des deux jours, deux lignes au Bilan aux bonnes dates, et le `+` de ligne sans effet sur `pj:`).
- v1.23 : (1) **tampon « FAIT » dans Favoris** — `ligneFav()` ajoute `.tampon` (encre `--sapin`, `rotate(-8deg)`, opacité .72) et la classe `.fv-fait` (nom atténué) quand `done:<id>` est vrai. (2) **classement des favoris rappelé au Bilan** : `ligneBilan()` appelle `sigleFav()` sur les lignes dont la clé commence par `done:` — rien sur les activités de planning ni sur les jeux (nom modifiable). (3) **menu des jeux déjà joués** (`#seljs`, action `jspart`) au-dessus du champ de saisie, affiché seulement s'il y a des jeux, chaque option portant son nombre de parties : une partie de plus sans ressaisir, donc plus de « Uno / UNO / uno » en trois entrées. (4) **Deux nouveaux paliers** appendus en fin de `MISSIONS` : `md:13` (20 jeux différents) et `md:14` (30 parties). `JSEUILS` porte un champ `c` (`d`/`p`) qui sert à trier les jauges. **v1.24 (demande utilisateur) : les quatre paliers ont chacun leur curseur**, groupés par compteur et du plus petit seuil au plus grand ; un palier atteint prend ✅, la barre passe en `--soleil` et la ligne s'atténue. La première version n'affichait que le palier en cours (`jalonCourant()`, retirée depuis) : les deux nouveaux seuils n'avaient alors aucun curseur tant que le premier n'était pas franchi. Le test `jalons.mjs` du scratchpad vérifie la bascule 10→20 et 20→30.
- v1.22 : **cascade d'ouverture** (demande utilisateur du 02/08) — au premier affichage, en-tête (0 ms) → barre de score (80) → barre des jours et cartes (160, 230, 300, 370, 440, 510) → onglets (580) montent de 18 px en se révélant (`monteFade`, 500 ms, `cubic-bezier(.2,.7,.3,1)`). **Le minutage est en CSS (`body.ouverture` + `#app > *:nth-child(-n+6)`), pas en JS** : la première synchro re-rend `#app` dans la foulée et effaçait des classes posées à la main — la première version, mesurée, ne cascadait pas du tout (carte pleine opacité dès 60 ms). Le `opacity:0` de départ vit **uniquement** dans `@media (prefers-reduced-motion:no-preference)` : sinon la règle globale `animation:none!important` du mode réduit laissait la page vide. `body.ouverture` est retirée à 1,4 s (dernier palier 580 + 500 ms) et sert aussi de filet de sécurité. `animerEntree()` sort immédiatement au premier rendu (drapeau `ouvertureFaite`) pour ne pas mêler `.rev`/`.vu` à la cascade ; changements d'onglet et rafraîchissements de synchro gardent leur comportement d'avant. Vérifié image par image par `film-ouverture.mjs` du scratchpad (16 contrôles : opacité et translation de chaque bloc toutes les 60 ms, ordre des paliers, étalement, rien de masqué en mode animations réduites).
- v1.21 : **icône du site** (demande utilisateur du 02/08). SVG à plat dessiné avec la palette du projet (fond `--encre`, soleil `--soleil`, massif `--papier`, pré `--sapin`, tente `--braise`) : deux sommets, soleil en haut à droite, tente au pied de la pente. 426 octets, aucune dépendance. **Servie en fichier, pas en data URI** — iOS ignore les data URI pour `apple-touch-icon`, or c'est l'usage principal ici (le README demande d'ajouter le QG à l'écran d'accueil). `server.js` sert `/icone.svg`, `/icone-180.png` et `/icone-512.png` (`Cache-Control: max-age=86400`, 404 JSON sinon) ; les PNG sont rendus depuis le SVG par Chromium (`rendu-icone.mjs` du scratchpad, avec planche de contrôle 16/32/64/120 px + mises en situation onglet et écran d'accueil). `<head>` : `icon` SVG + repli PNG, `apple-touch-icon`, `theme-color` `#23324A`, `apple-mobile-web-app-title` « Le QG », `description`, `robots noindex` (l'URL ne doit pas être indexée, elle vaut clé d'accès). **`apple-mobile-web-app-capable` volontairement absent** : en mode plein écran iOS il n'y a plus de bouton recharger, or le plan gratuit Render s'endort — mieux vaut garder la barre Safari.
- v1.20 : **audit de cohérence score / missions / planning / Bilan** (demande utilisateur du 02/08) — et **une vraie incohérence trouvée et corrigée**. `autoMissionsJeux()` n'était appelée que par `majJeux()`, donc les missions à seuil (`md:11`, `md:12`) n'étaient évaluées que sur l'appareil qui touchait un jeu. Un téléphone recevant l'état par la synchro pouvait afficher **21/20 avec la mission non cochée**, et le score s'en trouvait faussé de 2 points. L'appel est **déplacé en tête de `rendre()`** : le seuil est désormais réévalué à chaque rendu, donc aussi sur l'état reçu, quel que soit le chemin d'écriture. Pas de récursion — `autoMissionsJeux()` écrit directement dans `ST.state` et ne rend rien, et renvoie `[]` dès que la mission est acquise. Invariants désormais testés (suite e2e + `audit-coherence.mjs` du scratchpad) : `score()` = recalcul indépendant depuis les clés brutes ; total des points du Bilan = score de l'en-tête ; une ligne de Bilan par coche (ids partagés dédoublonnés) ; aucune ligne en double ni sans date ; compteurs des onglets Sorties/Randos/Missions/Bilan alignés ; missions retirées (`null`) cochées dans un état ancien ignorées partout ; seuil franchi sur un téléphone → mission cochée sur l'autre.
- v1.19 : **jeux corrigeables et ajoutables depuis le planning et le Bilan** (demande utilisateur du 02/08). **Nouveau type d'entrée `pj` : `{j:<idJeu>}`** à côté de `{i:<idSortie>}` et `{x:<texte>}` — la ligne rend le nom courant lu dans `js`, et la clé de coche `ca:<date>:p:--:<idJeu>` porte l'**id**, pas le libellé. C'était indispensable : avec l'ancien `{x:"🎲 <nom>"}`, `cleCA()` sluggait le libellé, donc **renommer aurait perdu la coche**. Les anciennes entrées `{x:"🎲 …"}` restent rendues comme texte libre (pas de migration). Le nom est un `<input data-act="jsnom">` dans les trois vues : bloc Jeux, ligne du programme du jour, ligne du Bilan (`r.jeu` porté par `realisations()`, icône 🎲 et sous-titre « Jeu de société »). Nouveau champ `#njp` dans la zone d'ajout du programme : enregistre le jeu (ou +1 partie s'il existe) **et** le pose sur la journée. `pjdel` accepte `it.j`.
- v1.18 : **correction d'une saisie de jeu** (demande utilisateur du 02/08). Le nom devient un `<input class="js-nom" id="jsn-<id>">` modifiable sur place (validé au `change` ou par Entrée) ; boutons **−** et **+** de part et d'autre. `partieDePlus(id, d)` prend un delta et **plancher à 1 partie** (le − est `disabled` à 1 ; ✕ reste la suppression). `renommerJeu()` : nom vide ou inchangé → rendu simple (l'ancien nom revient), nom identique à un autre jeu (casse ignorée) → **fusion des deux compteurs** + bandeau, cohérent avec la saisie qui traite déjà « uno » et « Uno » comme un seul jeu. **Nouvel utilitaire `escA()`** : `esc()` n'échappe ni `"` ni `'`, or le nom du jeu part désormais dans un attribut `value` — un nom avec guillemets aurait cassé le HTML. `rendre()` préserve les champs `jsn-*` comme les `dt-*`.
- v1.17 : **reprise des jeux déjà cochés** (demande utilisateur du 02/08 : « peux-tu compter les jeux déjà cochés »). Un bouton « ↻ Reprendre N jeux déjà cochés (…) » apparaît sous la saisie **uniquement s'il y a matière**, et nomme ce qu'il va reprendre avant l'appui. `jeuxDejaCoches()` balaie `catalogueCA()` restreint aux entrées effectivement cochées, retient celles dont le libellé matche `JEUXMOTIF` (`loup-garou|kapla|jeux en bois|puzzle|undercover|mölkky|jeux de société|jeux de cartes`) ou commence par `🎲`, et **compte une partie par jour** — le même jeu coché chez les enfants et chez les ados le même soir reste une seule partie. `nomJeu()` normalise (`Loup-garou (+8 ans)` → `Loup-garou`, `Tournoi de puzzle en famille` → `Tournoi de puzzle`) pour que la fusion par nom fonctionne. **Motif volontairement étroit** : il écarte « Jeux musicaux piscine », « Grand jeu aquatique », « Jeux en ligne », « Jeux avec Yellito », « Bricolage jeu de billes », « Tournoi FIFA » — mieux vaut en oublier (ajout manuel disponible) que compter une partie de piscine. Les jeux déjà dans `js` ne sont jamais reproposés.
- v1.16 : **jeux de société** (demande utilisateur du 02/08). Nouvelle clé partagée `js` = `[{id, n:nom, p:parties}]`, alimentée par un bloc **🎲 Jeux de société** sous les missions : champ « Nom du jeu… » (Entrée ou +), bouton **+1** par ligne, ✕ pour retirer. **Retaper un nom déjà présent (casse ignorée) ajoute une partie, pas un jeu** — c'est ce qui sépare les deux compteurs. Deux missions ajoutées **en fin de tableau** (`md:11` 10 jeux différents, `md:12` 20 parties) : ajouter à la fin ne réindexe rien, contrairement à une insertion. `JSEUILS` porte cible + accesseur ; `autoMissionsJeux()` (appelée par `majJeux()`) coche dès le seuil atteint avec la cascade de bandeaux, et **ne dé-coche jamais** — retirer un jeu fait redescendre le compteur mais l'objectif reste acquis. Compteur `X/Y` affiché en pastille sur les deux missions et en deux jauges dans le bloc ; **jamais plafonné** (21/20 s'affiche tel quel). Les jeux enregistrés apparaissent aussi dans le menu du **programme du jour** (groupe `🎲 Jeux de société`), ajoutés comme activité libre `🎲 <nom>` — valeur d'option préfixée `jeu:` car un id de sortie peut commencer par `j` (`jardinbains`). Aucun point par partie : seules les deux missions en rapportent.
- v1.15 : **menu déroulant d'ajout de mission** (demande utilisateur du 02/08) — la vue Missions propose désormais, au-dessus du champ libre, un `<select id="selm">` groupé par catégorie de sorties, `🥾 Randos` et `🏕️ Activités du camping` (`MISSCAMP()` : libellés du planning dédoublonnés et triés, mémoïsés). **La mission créée garde le lien vers l'élément choisi** : `{lien, lienLb}` dans l'objet `cm` — `lien` = id de sortie/rando (`lienLb:false`) ou libellé d'activité (`lienLb:true`, donc valable quel que soit le jour). `autoMissionsPerso()` est appelée par `coche()` à côté de `autoMissions()` : cocher la sortie, la rando ou la ligne de planning coche la mission et déclenche la même cascade de bandeaux. Jamais dé-cochée automatiquement, décochage manuel toujours possible. Un élément déjà réalisé crée une mission déjà cochée (`estDejaFait()`), et les entrées déjà liées disparaissent du menu. **Les trois endroits qui reconstruisaient l'objet mission (`dtcm` ×2, `cm`) passent par `Object.assign({}, m, …)`** — sinon `lien`/`lienLb` étaient perdus à la première correction de date ou au premier décochage. Valeurs d'option encodées `i<id>` / `c<index>` : `esc()` n'échappe pas les guillemets, or les libellés en contiennent. Règle d'icône `🎉` ajoutée pour les jeux et soirées du planning.
- v1.14 : **icône automatique sur les missions ajoutées par la famille** (demande utilisateur du 02/08). `ICOMISS` = liste ordonnée de motifs → emoji (🪂 parapente, 🛷 luge, 🦅 rapaces, 🧺 marché/goûter, 📸 photo, 🎨 créa, 🏊 piscine, 🥾 rando, ⛰️ sortie/site, 🏸 sport), repli ⭐. `icoMission()` renvoie "" si le libellé commence déjà par un emoji (`\p{Extended_Pictographic}`, avec repli si la classe Unicode n'est pas supportée) pour ne pas le dédoubler. **L'icône est calculée à l'affichage : `libMission()` est appelée dans `vueMissions()`, `realisations()` (Bilan) et le bandeau de `feteMission()` — le libellé enregistré dans `cm` n'est jamais modifié**, sinon l'emoji se figerait dans le texte partagé et se cumulerait à chaque rendu.
- v1.13 : (1) **deux missions retirées** (demande utilisateur du 02/08) — `md:1` tir à l'arc et `md:6` souvenir fait main (doublon de la mission créa `md:5`). **L'index d'une mission EST sa clé partagée : le tableau `MISSIONS` n'est jamais réindexé**, une entrée retirée devient `null` et est ignorée par `score()`, `realisations()`, `vueMissions()` et le gestionnaire `md` — sinon les coches déjà faites glisseraient sur d'autres missions. 9 missions actives, tableau toujours long de 11. `MISSLIEN` : les deux liens correspondants supprimés, la regex de `md:5` absorbe `marque-page|gratter` pour ne pas perdre l'auto-validation. (2) **17e rando : cirque de Gavarnie → hôtellerie du Cirque** (8-9 km A/R, +220 m, 1 350 → 1 570 m, 2 h 30-3 h 30, dès 4-5 ans, 6 étapes, option pied de la Grande Cascade). Elle **partage son id avec la fiche Sortie `gavarnie`** (comme arrieulat et balandrau) : une seule coche, un seul vote, pas de repère en double sur la carte, et la fiche Sortie affiche automatiquement le bouton « 🥾 Fiche rando détaillée ». Comble le seul secteur absent des randos (vallée de Luz-Gavarnie) ; `g:"auto"` passe de « ≤ 40 min » à « ≤ 1 h » (Gavarnie à 50 min).
- v1.12 : **semaine 2 injectée** d'après les photos du panneau du 01/08. `CAMP` couvre désormais 02/08 → 07/08 pour les 3 publics (+98 lignes, 219 au total) ; `SOIREES` reçoit la semaine type affichée sur les affiches « Soirées / Show time » (dim. 21h00 Show de bienvenue · lun. Soirée mousse · mar. Music-Hall Party (Prod2Be) · mer. Soirée dansante · jeu. Dance Factory — 21h30). **Vendredi 07/08 : emplacement vide sur le panneau → aucune soirée inventée, `[à vérifier]` porté dans la note du jour.** Le 01/08 garde la version du panneau semaine 1 (surensemble : elle ajoute Football 17h côté adultes). `NOTES` réécrites pour la semaine 2 (show de bienvenue, tirage au sort du lundi, sortie Parc animalier du mercredi, journée famille du vendredi). Le message « programme pas encore affiché » est remplacé par un message de jour du départ (seul le 08/08 est désormais sans planning). Mission `md:1` (tir à l'arc) : les horaires semaine 1 retirés du libellé — l'activité n'apparaît pas sur le panneau semaine 2, `[à vérifier]`.
- v1.11 : (1) **rappel du classement Favoris** — `classementFav()` calcule le rang parmi les activités ayant au moins un vote, `sigleFav(id)` rend une pastille 🥇🥈🥉 (podium) ou `#N` (title = nombre de votes), affichée sur les idées du plan météo, du programme du jour (`ligneIdee()`) et sur les fiches Sorties/Randos. (2) La zone d'ajout du programme est **repliée par défaut** derrière un bouton « ➕ Ajouter une activité » (état local `ouvertAjoutPJ`, non synchronisé) pour raccourcir la vue Semaine. (3) **Animations** : transition d'entrée `#app.swap` au seul changement d'onglet (jamais sur un rafraîchissement de synchro, sinon clignotement), révélation des cartes au défilement via IntersectionObserver (classes `.rev`/`.vu`, filet de sécurité à 1,2 s pour ne jamais laisser un bloc invisible), en-tête qui se compacte au-delà de 28 px de défilement (`body.defile`), barre de progression `#prog` dans l'en-tête, rebond de l'icône d'onglet actif et retour en haut fluide au changement d'onglet. Tout est neutralisé par `prefers-reduced-motion`.
- v1.7.3 : Fête du Sud-Ouest déplacée du 26 au mercredi 29/07 (correction utilisateur) ; la carte ne reconstruit plus ses repères tant qu'une popup est ouverte (la synchro périodique fermait la bulle en cours de lecture).
- v1.6.1 : les plans de journée (vue Semaine, sélecteur météo) proposent aussi les randos adaptées au créneau — grand beau : lac de Gaube + chemin des Cascades ; orages : lac de Soum + crêtes du Hautacam (matinées courtes) ; canicule : plateau du Clot + cascade de Lutour (fraîcheur) ; pluie : volontairement sans rando. Chaque ligne du plan est tactile et ouvre la fiche correspondante (sortie ou rando).
- v1.5.1 : les 8 missions logistiques remplacées par **11 défis famille** mêlant 🏸 sport / 🎨 créa / ⛰️ sorties (demande utilisateur du 26/07), tous adossés à des activités réelles du planning camping ou des fiches ; mécanique inchangée (clés `md:<index>`, 2 pts par mission, missions perso conservées). Ramenés à **9** en v1.13 — sans réindexation.
- v1.4 : onglet **⭐ Favoris** — liste des membres de la famille partagée (clé `mb`, prénoms ajoutés par la famille elle-même, jamais inventés), votes par membre (clés `fav:<membre>:<sortie>`), classement « Par votes » (podium 🥇🥈🥉, émojis des votants) ou « Par type » (par catégorie), compteur ❤️ reporté sur les fiches Sorties. L'identité du votant (`moi`) reste en mémoire de page — aucun stockage navigateur (invariant respecté) : on retouche son prénom après un rechargement. La RAZ globale efface aussi membres et votes.

## Backlog (non décidé)
- Soirée du vendredi 07/08 : emplacement vide sur le panneau — à relever sur place ou à l'accueil.
- **Limite connue (préexistante)** : `cm` (missions perso) et `mb` (membres) sont des tableaux stockés sous une seule clé. La fusion étant par clé, deux appareils qui ajoutent au même moment écrasent l'un l'autre — le dernier écrit gagne et l'autre ajout est perdu. Correctif possible sans toucher au contrat : fusionner par `id` côté front avant d'écrire.
- Météo automatique (Open-Meteo) en plus du choix manuel.
- QR code de l'URL à imprimer pour la famille.
- Endormissement du plan gratuit Render : décider (accepter / cron de réveil / plan payant / retour Vercel).
- Fin de l'essai PostgreSQL (~fin août) : sans enjeu après le séjour (08/08) — supprimer la base, ou migrer si l'appli doit survivre.
